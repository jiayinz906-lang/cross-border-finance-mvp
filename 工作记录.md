# 工作记录

## 2026-07-29 ERP 手工业务单替代日常 Excel 导入

- 将“原始数据录入”升级为“业务数据录入”，日常数据改为 ERP 式手工业务单，不再从经营总览提供 Excel 上传入口。
- 业务单采用“单据头 + 多条费用明细 + 图片凭证”结构，字段覆盖 `表头模版.xlsx` 的 23 列规范；支持草稿、确认入账、详情追溯和填写原因后作废。
- 扩展 `ManualLedgerEntry`，增加单据号、行号、客户、费用类型、供应商服务、重量、件数、主品名、内部备注、折合人民币等字段；新增 `ImportBatch.sourceType` 区分手工业务单与历史 Excel。
- 确认或作废业务单时，后端按当前月份所有有效手工业务单重新生成 `RawLedgerLine`、`FinanceChargeLine`、`FinanceOrder`、`FinanceSummary`、风险、提成和注册服务确认记录。
- 录入金额、币种、汇率和正负号由业务单原始费用行保留，财务汇总继续复用既有统一计算口径，确保应收、应付和毛利可逐行追溯。
- 参数规则页将数据批次统一显示为“业务数据批次”，标明“手工业务单/历史 Excel”来源；历史 Excel 表头和导入接口仅保留用于旧数据迁移与兼容回归。
- 本地 Docker PostgreSQL 已应用迁移 `20260729000100_manual_erp_documents`。
- 完成 API 闭环验证：创建含 2 条费用行和图片凭证的业务单，应收 `1000`、应付 `600`、毛利 `400`；确认入账后总览一致，作废后自动重算。验证数据已清理。
- 本地前端 `http://localhost:5173/`、后端 `http://localhost:4000/api`、PostgreSQL 容器均正常运行。

## 2026-07-28 腾讯云生产化准备

- 保留现有财务计算、导入、签名、锁账、角色和历史数据逻辑。
- 新增腾讯云专用 Compose、HTTPS Nginx 模板和生产环境示例。
- 增加维护模式、登录/签名限流、Helmet、安全审计字段和版本健康信息。
- 增加数据库迁移 manifest、只读预检、测试恢复和生产只读验收工具。
- 增加本地/COS 备份、systemd 定时任务、发布、状态和应用回滚脚本。
- 增加腾讯云部署、迁移、备份恢复、发布回滚、灾难恢复、安全和事故响应文档。
- 未连接或修改 Render 正式数据库；未执行真实腾讯云迁移；未删除任何数据库或卷。

### 本地隔离演练结果

- 使用独立项目 `xjd-finance-tencent-test` 启动 PostgreSQL 17、后端和 Nginx，端口仅绑定到 `127.0.0.1:15439/18080/18443`。
- 发现并修复 migration 容器在无交互环境中通过 pnpm 启动会尝试重建依赖的问题，改为直接运行已安装的 tsx CLI；3 个 Prisma migration 均成功应用。
- 隔离数据库完成标准 Excel 导入：25 单、102 原始费用行、102 标准费用行；应收、应付和毛利对账通过。
- `verify:all` 全量验证通过，覆盖角色权限、导入、风险、确认单、签名证据、月结、回滚、原始流水、图片、收付款和 UI 冒烟测试。
- 生成 custom-format PostgreSQL 备份，`pg_restore --list` 验证通过；恢复到独立 `xjd_finance_restore` 数据库成功。
- 源库与恢复库 manifest 比对：52 项一致、0 警告、0 阻断；订单、金额、原始行、确认单、签名证据、锁账和 migration 校验和一致。
- HTTPS、前端、`/api/health`、登录和维护模式验证通过；本地证书为演练自签名证书，不用于正式环境。
- `pnpm build`、`pnpm build:render`、`pnpm verify:operations`、`pnpm verify:maintenance` 通过。

### 尚未执行

- 未连接 Render 正式数据库，未生成正式源库 manifest 或正式备份。
- 未在腾讯云 CVM 创建目录、写入环境变量、配置证书、COS、DNS、安全组或监控。
- 未切换流量，也未修改 GitHub Pages/Render 线上环境。
- Prisma 中仍有 64 个 Float 金额字段；本阶段按约束不修改数据库表结构，需单独设计 Decimal 迁移方案。

## 2026-07-30 腾讯云隔离部署
- 本地前后端构建、Render 构建与 doctor 验证通过。
- 已提交并推送部署版本 `1061e3b feat: prepare production self-hosted deployment` 到 GitHub 主分支。
- 腾讯云服务器环境已完成只读核验：现有 Nginx/MySQL/Redis/Java/宝塔服务运行，80 端口占用；2GB 内存紧张且 1GB Swap 已满。
- 已确认后续采用 `/opt/xjd-finance`、独立 PostgreSQL 数据卷、18080 端口与新增 3GB Swap 的隔离部署方案。
- 当前待办：恢复 Chrome 终端控制连接后，执行 Swap、拉取代码、生成生产环境变量、Docker Compose 启动及 18080 防火墙开放，并验证公网健康检查。
